
// Example: Track the email opened in Gmail
if (window.location.hostname === 'mail.google.com') {
  console.log("Gmail opened, ready for follow-up tracking.");
  // Add additional logic to interact with Gmail's DOM if needed
}
